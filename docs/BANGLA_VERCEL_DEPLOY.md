# Campus Dirasa - Vercel ডিপ্লয়মেন্ট গাইড (বাংলা)

**সম্পূর্ণ বাংলায় স্টেপ বাই স্টেপ গাইড**

---

## 📋 শুরু করার আগে যা লাগবে

- ✅ GitHub একাউন্ট (আপনার কোড এখানে আছে)
- ✅ Namecheap ডোমেন (ইতিমধ্যে কিনে রাখা)
- ✅ ১৫ মিনিট সময়

---

## ধাপ ১: Vercel একাউন্ট তৈরি করুন (২ মিনিট)

### ১.১ Vercel ওয়েবসাইটে যান

1. ব্রাউজারে যান: **https://vercel.com**
2. উপরে ডানদিকে **"Sign Up"** বাটনে ক্লিক করুন

### ১.২ GitHub দিয়ে সাইন আপ করুন

1. **"Continue with GitHub"** বাটনে ক্লিক করুন
2. আপনার GitHub ইউজারনেম এবং পাসওয়ার্ড দিন
3. **"Sign in"** ক্লিক করুন
4. যদি জিজ্ঞেস করে **"Authorize Vercel"** তাহলে ক্লিক করুন

**✅ এখন আপনি Vercel এ লগইন হয়ে গেছেন!**

---

## ধাপ ২: GitHub Repository Import করুন (৩ মিনিট)

### ২.১ নতুন প্রজেক্ট তৈরি করুন

1. Vercel Dashboard এ আপনি এখন আছেন
2. **"Add New..."** বাটন খুঁজুন (উপরে ডানদিকে)
3. ক্লিক করুন **"Add New..." → "Project"**

### ২.২ GitHub Repository খুঁজুন

আপনি এখন দেখবেন **"Import Git Repository"** পেজ।

**যদি আপনার `campusdirasa` রিপোজিটরি দেখতে না পান:**

1. **"Adjust GitHub App Permissions →"** লিংকে ক্লিক করুন
2. নতুন পেজ খুলবে (GitHub)
3. **"Only select repositories"** সিলেক্ট করুন
4. **"Select repositories"** ড্রপডাউন থেকে **"campusdirasa"** খুঁজুন
5. **"Save"** ক্লিক করুন
6. Vercel ট্যাবে ফিরে যান এবং রিফ্রেশ করুন

### ২.৩ Repository Import করুন

1. এখন **"campusdirasa"** রিপোজিটরি দেখবেন
2. তার পাশে **"Import"** বাটনে ক্লিক করুন

---

## ধাপ ৩: প্রজেক্ট কনফিগার করুন (৩ মিনিট)

### ৩.১ প্রজেক্টের নাম দিন

1. **Project Name:** `campusdirasa` (অথবা আপনার পছন্দের নাম)
2. **Framework Preset:** Next.js (অটোমেটিক ডিটেক্ট হবে)
3. **Root Directory:** `./` (চেঞ্জ করার দরকার নেই)

### ৩.২ Environment Variables যোগ করুন (গুরুত্বপূর্ণ!)

**এই স্টেপটা অবশ্যই করতে হবে!**

1. **"Environment Variables"** সেকশন খুঁজুন
2. **"Add"** বাটনে ক্লিক করুন (বা expand করুন)

**প্রথম Variable যোগ করুন:**

```
Name: NEXTAUTH_SECRET
Value: [নিচের কমান্ড দিয়ে generate করুন]
```

**NEXTAUTH_SECRET Generate করার উপায়:**

**Windows এ:**
1. Git Bash ওপেন করুন (অথবা PowerShell)
2. টাইপ করুন:
   ```bash
   openssl rand -base64 32
   ```
3. যা আসবে সেটা কপি করুন (যেমন: `dGhpc2lzYXNlY3JldGtleQ==`)

**অনলাইনে generate করুন:**
- যান: https://generate-secret.vercel.app
- **"Generate"** ক্লিক করুন
- কপি করুন

**এখন Vercel এ পেস্ট করুন:**
1. **Name** ফিল্ডে লিখুন: `NEXTAUTH_SECRET`
2. **Value** ফিল্ডে পেস্ট করুন যা generate হয়েছে
3. **Environment** এ তিনটাই চেক করুন: Production, Preview, Development

**দ্বিতীয় Variable যোগ করুন:**

1. আবার **"Add"** ক্লিক করুন
2. **Name:** `NEXTAUTH_URL`
3. **Value:** `https://campusdirasa.vercel.app`

   ⚠️ **গুরুত্বপূর্ণ:** এখানে আপনার Vercel subdomain লিখুন।
   - যদি Project Name: `campusdirasa` তাহলে URL হবে: `https://campusdirasa.vercel.app`
   - আপনার project name অনুযায়ী চেঞ্জ করুন

4. তিনটাই চেক করুন: Production, Preview, Development

### ৩.৩ Deploy করুন!

1. সব চেক করে নিন
2. নিচে **"Deploy"** বাটনে ক্লিক করুন
3. অপেক্ষা করুন ২-৩ মিনিট ⏳

**ডিপ্লয় হচ্ছে... স্ক্রিনে দেখবেন:**
- Building... (১-২ মিনিট)
- Deploying... (৩০ সেকেন্ড)
- ✅ Congratulations! 🎉

---

## ধাপ ৪: আপনার সাইট চেক করুন (১ মিনিট)

### ৪.১ সাইট ভিজিট করুন

1. ডিপ্লয় শেষ হলে দেখবেন **"Visit"** বাটন
2. ক্লিক করুন
3. নতুন ট্যাবে আপনার সাইট খুলবে!

**URL হবে:** `https://campusdirasa.vercel.app`

### ৪.২ টেস্ট করুন

1. হোমপেজ লোড হচ্ছে কিনা দেখুন
2. লগইন পেজে যান: `/login`
3. নেভিগেশন কাজ করছে কিনা চেক করুন

**যদি সব ঠিক থাকে → এগিয়ে যান ধাপ ৫ এ!**

---

## ধাপ ৫: Namecheap ডোমেন কানেক্ট করুন (৫ মিনিট)

### ৫.১ Vercel এ Domain যোগ করুন

1. Vercel Dashboard এ ফিরে যান
2. আপনার **campusdirasa** প্রজেক্টে ক্লিক করুন
3. উপরে **"Settings"** ট্যাবে ক্লিক করুন
4. বাম পাশে **"Domains"** ক্লিক করুন

### ৫.২ ডোমেইন নাম এন্টার করুন

1. একটা ইনপুট বক্স দেখবেন **"Enter domain name"**
2. আপনার ডোমেইন টাইপ করুন: `campusdirasa.com` (আপনার ডোমেইন নাম দিন)
3. **"Add"** বাটন ক্লিক করুন

### ৫.৩ DNS Records নোট করুন

Vercel এখন আপনাকে দেখাবে কি কি DNS records যোগ করতে হবে:

```
To configure campusdirasa.com, add these records:

A Record:
  Name: @
  Value: 76.76.21.21

CNAME Record:
  Name: www
  Value: cname.vercel-dns.com
```

**এই পেজটা খোলা রাখুন!** পরের ধাপে লাগবে।

---

## ধাপ ৬: Namecheap এ DNS Configure করুন (৫ মিনিট)

### ৬.১ Namecheap এ লগইন করুন

1. নতুন ট্যাবে যান: **https://namecheap.com**
2. উপরে ডানদিকে **"Sign In"** ক্লিক করুন
3. Username এবং Password দিন
4. লগইন করুন

### ৬.২ Domain List এ যান

1. লগইন হলে বাম পাশে মেনুতে **"Domain List"** ক্লিক করুন
2. আপনার ডোমেইন দেখবেন (যেমন: `campusdirasa.com`)
3. তার ডান পাশে **"Manage"** বাটনে ক্লিক করুন

### ৬.৩ Advanced DNS এ যান

1. উপরে কয়েকটা ট্যাব দেখবেন: Details, Products, AutoRenew, etc.
2. **"Advanced DNS"** ট্যাবে ক্লিক করুন

### ৬.৪ পুরানো Records মুছে ফেলুন (যদি থাকে)

**চেক করুন:**
- যদি **Host** `@` বা **Host** `www` এর কোনো record থাকে
- যেগুলো **Namecheap Parking Page** বা **URL Redirect** দেখায়

**মুছে ফেলুন:**
1. ডান পাশে **ট্র্যাশ আইকন** 🗑️ দেখবেন
2. ক্লিক করুন প্রতিটা পুরানো record এর জন্য
3. Confirm করুন

**⚠️ মনে রাখবেন:** MX records (ইমেইলের জন্য) মুছবেন না!

### ৬.৫ প্রথম Record যোগ করুন (A Record)

1. **"Add New Record"** বাটনে ক্লিক করুন
2. একটা ফর্ম দেখবেন

**ফর্মে এগুলো দিন:**

| ফিল্ড | মান | ব্যাখ্যা |
|-------|-----|---------|
| **Type** | `A Record` | ড্রপডাউন থেকে সিলেক্ট করুন |
| **Host** | `@` | @ মানে root domain |
| **Value** | `76.76.21.21` | Vercel এর IP |
| **TTL** | `Automatic` | ডিফল্ট রাখুন |

3. সবুজ **চেকমার্ক** ✓ এ ক্লিক করুন (Save)

### ৬.৬ দ্বিতীয় Record যোগ করুন (CNAME Record)

1. আবার **"Add New Record"** ক্লিক করুন

**ফর্মে এগুলো দিন:**

| ফিল্ড | মান | ব্যাখ্যা |
|-------|-----|---------|
| **Type** | `CNAME Record` | ড্রপডাউন থেকে সিলেক্ট করুন |
| **Host** | `www` | www subdomain এর জন্য |
| **Value** | `cname.vercel-dns.com` | Vercel এর CNAME |
| **TTL** | `Automatic` | ডিফল্ট রাখুন |

2. সবুজ **চেকমার্ক** ✓ এ ক্লিক করুন (Save)

### ৬.৭ সব Change Save করুন

1. উপরে একটা সবুজ বাটন দেখবেন: **"Save All Changes"**
2. ক্লিক করুন!

**✅ DNS Configuration সম্পন্ন!**

এখন আপনার DNS records এরকম দেখাবে:

```
Type          Host    Value                      TTL
----------------------------------------------------------
A Record      @       76.76.21.21               Automatic
CNAME Record  www     cname.vercel-dns.com      Automatic
```

---

## ধাপ ৭: DNS Propagation এর জন্য অপেক্ষা করুন (৫-১০ মিনিট)

### ৭.১ DNS চেক করুন

1. নতুন ট্যাবে যান: **https://dnschecker.org**
2. আপনার ডোমেইন টাইপ করুন: `campusdirasa.com`
3. **Type:** `A` সিলেক্ট করুন
4. **"Search"** ক্লিক করুন

**দেখবেন:**
- সবুজ চেকমার্ক ✓ মানে DNS propagate হয়েছে
- লাল ক্রস ✗ মানে এখনো হয়নি (অপেক্ষা করুন)

### ৭.২ Vercel এ Verification চেক করুন

1. Vercel ট্যাবে ফিরে যান (যেখানে Domains পেজ খোলা আছে)
2. পেজ রিফ্রেশ করুন
3. দেখবেন স্ট্যাটাস:
   - 🟡 **"Pending Verification"** - এখনো চেক হচ্ছে
   - 🟢 **"Valid Configuration"** - সফল!
   - 🔴 **"Invalid Configuration"** - কিছু ভুল আছে

**যদি "Valid Configuration" দেখেন → এগিয়ে যান!**

---

## ধাপ ৮: NEXTAUTH_URL আপডেট করুন (২ মিনিট)

এখন আপনার কাস্টম ডোমেইন কানেক্ট হয়ে গেছে, তাই environment variable আপডেট করতে হবে।

### ৮.১ Environment Variables এ যান

1. Vercel প্রজেক্ট ড্যাশবোর্ডে থাকুন
2. **"Settings"** ট্যাবে ক্লিক করুন
3. বাম পাশে **"Environment Variables"** ক্লিক করুন

### ৮.২ NEXTAUTH_URL এডিট করুন

1. **NEXTAUTH_URL** খুঁজুন
2. ডান পাশে **পেন্সিল আইকন** ✏️ (Edit) ক্লিক করুন
3. **Value** চেঞ্জ করুন:

   **পুরানো:** `https://campusdirasa.vercel.app`

   **নতুন:** `https://campusdirasa.com` (আপনার ডোমেইন)

4. **"Save"** ক্লিক করুন

### ৮.৩ Redeploy করুন

1. উপরে **"Deployments"** ট্যাবে ক্লিক করুন
2. সবচেয়ে উপরের deployment খুঁজুন
3. ডান পাশে **তিন ডট** ⋯ আইকনে ক্লিক করুন
4. **"Redeploy"** সিলেক্ট করুন
5. **চেকবক্স** চেক করুন: "Use existing Build Cache"
6. **"Redeploy"** বাটন ক্লিক করুন

**অপেক্ষা করুন ১-২ মিনিট...**

---

## ধাপ ৯: আপনার ডোমেইন টেস্ট করুন! (২ মিনিট)

### ৯.১ SSL সার্টিফিকেট চেক করুন

Vercel অটোমেটিক SSL সার্টিফিকেট দেয়। এটা ২-১০ মিনিট সময় নেয়।

**Domains পেজে দেখবেন:**
- SSL: 🟢 **Active** - সফল!
- SSL: 🟡 **Pending** - অপেক্ষা করুন

### ৯.২ আপনার সাইট ভিজিট করুন

এই URL গুলো টেস্ট করুন:

1. **http://campusdirasa.com**
   - অটোমেটিক HTTPS এ redirect হবে ✅

2. **https://campusdirasa.com**
   - সাইট লোড হবে ✅
   - সবুজ তালা 🔒 দেখবেন (SSL) ✅

3. **http://www.campusdirasa.com**
   - কাজ করবে ✅

4. **https://www.campusdirasa.com**
   - কাজ করবে ✅

### ৯.৩ Authentication টেস্ট করুন

1. `/login` পেজে যান
2. লগইন করার চেষ্টা করুন
3. ড্যাশবোর্ড অ্যাক্সেস করুন
4. সব ফিচার চেক করুন

---

## 🎉 অভিনন্দন! আপনার সাইট Live!

আপনার Campus Dirasa এখন এই ঠিকানায় লাইভ:

```
https://campusdirasa.com
```

---

## ✅ সম্পন্ন করার চেকলিস্ট

- [ ] Vercel একাউন্ট তৈরি করেছি
- [ ] GitHub repository import করেছি
- [ ] Environment variables যোগ করেছি
- [ ] প্রথমবার deploy হয়েছে
- [ ] Vercel subdomain এ সাইট কাজ করছে
- [ ] Namecheap এ DNS records যোগ করেছি
- [ ] DNS propagate হয়েছে
- [ ] SSL certificate active হয়েছে
- [ ] NEXTAUTH_URL আপডেট করেছি
- [ ] Redeploy করেছি
- [ ] কাস্টম ডোমেইনে সাইট লোড হচ্ছে
- [ ] HTTPS কাজ করছে
- [ ] Login/Authentication কাজ করছে
- [ ] সব পেজ লোড হচ্ছে

---

## 🐛 সমস্যা সমাধান

### সমস্যা ১: "Deploy Failed" / Build Error

**সমাধান:**
1. Vercel এ **Deployments** ট্যাবে যান
2. Failed deployment এ ক্লিক করুন
3. **Build Logs** দেখুন
4. Error মেসেজ পড়ুন

**সাধারণ সমস্যা:**
- Environment variables নেই → আবার যোগ করুন
- Node version ভুল → সাধারণত অটো হয়

### সমস্যা ২: DNS Propagate হচ্ছে না

**চেক করুন:**
1. Namecheap এ DNS records ঠিক আছে কিনা
2. `@` লিখেছেন কিনা (খালি না)
3. `www` লিখেছেন কিনা (`www.` না)
4. IP: `76.76.21.21` ঠিক আছে কিনা
5. CNAME: `cname.vercel-dns.com` ঠিক আছে কিনা

**সমাধান:**
- DNS records ডিলিট করে আবার যোগ করুন
- ৪৮ ঘন্টা পর্যন্ত অপেক্ষা করতে পারে (সাধারণত ১০ মিনিট)

### সমস্যা ৩: SSL Certificate Error

**"Your connection is not private" দেখাচ্ছে:**

1. আরো ১০ মিনিট অপেক্ষা করুন
2. Browser cache ক্লিয়ার করুন: `Ctrl + Shift + Delete`
3. Incognito mode এ চেষ্টা করুন
4. Vercel এ SSL status চেক করুন

### সমস্যা ৪: Login করতে পারছি না

**সমাধান:**
1. Browser এর সব cookies ক্লিয়ার করুন
2. NEXTAUTH_URL চেক করুন:
   - হতে হবে: `https://campusdirasa.com`
   - হবে না: `http://` বা `.vercel.app`
3. Redeploy করুন
4. আবার চেষ্টা করুন

### সমস্যা ৫: শুধু www কাজ করছে (অথবা শুধু non-www)

**সমাধান:**
1. Namecheap এ দুটো records-ই আছে কিনা চেক করুন:
   - A record (`@`)
   - CNAME record (`www`)
2. DNS propagation চেক করুন
3. Vercel এ উভয় domain add করুন

---

## 📞 সাহায্য দরকার?

**যদি এখনো সমস্যা হয়:**

1. **কোন ধাপে আটকে আছেন?**
2. **কি error দেখাচ্ছে?** (স্ক্রিনশট নিন)
3. **DNS চেক করেছেন?** (dnschecker.org)
4. **SSL active আছে?** (Vercel Domains পেজে)

আমাকে বলুন, আমি সাহায্য করব!

---

## 🎯 পরবর্তী পদক্ষেপ

সাইট লাইভ হওয়ার পর:

1. **Primary Domain সেট করুন**
   - Vercel Settings → Domains
   - `campusdirasa.com` কে primary করুন

2. **Google Search Console এ যোগ করুন**
   - SEO এর জন্য

3. **Analytics সেটআপ করুন**
   - Vercel Analytics enable করুন

4. **শেয়ার করুন!**
   - Students দের জানান
   - Social media তে পোস্ট করুন

---

**শুভকামনা! 🚀**
